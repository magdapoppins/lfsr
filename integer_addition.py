carry = 0

def integer_add(a, b):
    # What to do if two arrays of not equal length?
    if (len(a) != len(b)):
        return "No way"
    
    answer = []
    for i in range(len(a)):
        # If 0 and 1 just add 1 then check carrier
        if ()

def use_carry(bit_value):
    # If 0, just return 1 and set carrier to 0
    # If 1 return 0 and reset carrier to 1